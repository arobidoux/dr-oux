
const analyzeGame = require("./lib/analyzeGame");

/* TODO:
    x- use the player name as unique identifier
    x- add the player_uuid in the gamestats table
    - make a command that can run the stats for a single game
        - run that command when a game ends
    - look to have a connection pool of 1 to allow parallel processing of the stats
 */


